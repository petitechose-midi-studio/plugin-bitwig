# Bitwig Icons Configuration
# Used by: generate_bitwig_icons.sh

# SVG source directory (relative to project root)
SVG_SOURCE_DIR="asset/icon"

# Output TTF name (without extension)
TTF_NAME="bitwig_icons"
